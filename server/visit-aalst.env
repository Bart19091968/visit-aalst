# Server configuration
PORT=3000
# DATA_DIR: where JSON & uploads are stored (defaults to server/storage)
DATA_DIR=./storage
# UPLOAD_DIR: directory for uploaded photos (defaults to DATA_DIR/uploads)
UPLOAD_DIR=./storage/uploads
# BASE_URL: absolute base URL used in returned upload URLs (optional)
# e.g. BASE_URL=https://yourdomain.tld
# BASE_URL=
